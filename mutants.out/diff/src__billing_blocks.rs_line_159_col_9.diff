--- src/billing_blocks.rs
+++ replace BillingBlockManager::get_blocks_with_usage -> Vec<(NaiveDate, &BillingBlock)> with vec![]
@@ -151,20 +151,17 @@
         self.blocks
             .get(&today)?
             .iter()
             .find(|block| block.contains(now))
     }
 
     /// Get billing blocks with usage (non-empty blocks)
     pub fn get_blocks_with_usage(&self) -> Vec<(NaiveDate, &BillingBlock)> {
-        self.get_all_blocks()
-            .into_iter()
-            .filter(|(_, block)| block.usage.total_tokens() > 0)
-            .collect()
+        vec![] /* ~ changed by cargo-mutants ~ */
     }
 
     /// Calculate total usage across all billing blocks
     pub fn total_usage(&self) -> TokenUsage {
         let mut total = TokenUsage::default();
 
         for blocks in self.blocks.values() {
             for block in blocks {
