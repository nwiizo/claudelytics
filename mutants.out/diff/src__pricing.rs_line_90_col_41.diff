--- src/pricing.rs
+++ replace || with && in PricingFetcher::get_model_pricing
@@ -82,17 +82,17 @@
                 && let Some(pricing) = pricing_data.get(variation)
             {
                 return Some(pricing.clone());
             }
         }
 
         // Partial match as fallback
         for (key, pricing) in pricing_data {
-            if key.contains(model_name) || model_name.contains(key) {
+            if key.contains(model_name) && /* ~ changed by cargo-mutants ~ */ model_name.contains(key) {
                 return Some(pricing.clone());
             }
         }
 
         None
     }
 
     pub fn calculate_cost(
